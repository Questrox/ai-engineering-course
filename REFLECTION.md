### Модуль 1
1) При 2 запусках при температуре 0.0 ответы были очень похожи друг на друга и по структуре, и по формулировкам, а при 1.0 отличались. Кроме того, при температуре 0.0 ответы были более краткими и по делу.
2) При запуске с MAX_TOKENS = 50 ответ оборвался еще на первой причине, на полуслове, а также finish_reason была length, а не stop. Также было выведено предупреждение о том, что ответ был обрезан из-за лимита.
3) При использовании роли строгого технического специалиста ответ был очень сухим и кратким, потратилось всего лишь 58 токенов. При использовании роли наставника вернулся более подробный ответ с несколькими причинами проблем и рекомендациями, но на это потратилось больше токенов (310). Выбор роли тут зависит от того, кто конкретно пользуется LLM и то, сколько токенов должно расходоваться, но я бы выбрал роль наставника, т.к. в этом случае дается больше информации и больше вариантов того, что нужно проверить при поломке.

### Модуль 2
## Практика 1. Классификация обращений
============================================================
СРАВНЕНИЕ СТРАТЕГИЙ
============================================================
  zero-shot    11/12   (92%)
  few-shot     12/12   (100%)
  CoT          10/12   (83%)

На чём ошибались:
  [zero-shot] «В какой срок после остановки оборудования ...»
           ждали: регламент, получили: инцидент
  [CoT] «Линия розлива №3 встала, на пульте горит а...»
           ждали: инцидент, получили: регламент
  [CoT] «Пресс П-7 не выходит на рабочее давление, ...»
           ждали: инцидент, получили: регламент

Точнее всего работает стратегия few-shot, т.к. в данном случае у модели есть примеры, особенно для сложных обращений по типу карточки инцидента. В zero-shot примеров не было, поэтому модель зацепилась за слово "инцидент", хотя как такового инцидента в обращении нет. В CoT модель запуталась именно из-за предложения "Не путай цель обращения с его целью: если в тексте упомянута авария, но человек спрашивает про порядок действий - это регламент, а не инцидент" и ошибочно посчитала 2 аварии за регламент. После изменения этого предложения на "Например, если в обращении есть слово "инцидент" или "регламент", то это не значит, что они автоматом попадают под эту категорию, смотри именно на общий смысл" точность повысилась.

Один прогон потратил 2636 токенов (с учетом CoT, т.е. 36 вызовов). Если не брать в учет разный расход токенов у разных стратегий, то каждый вызов стоит в среднем 73 токена. Если взять 1000 обращений в день, то получится 7300 токенов в день. С учетом ограничений в 1 миллион токенов на бесплатном плане токены закончатся через 137 дней.